# math.min(value...) return min value
# math.max(value....) return max value
# math.abs(-value) return absolute positive value
# math.pow(x,y) return x^y
# math.sqrt(value) return root
# math.ceil(value) rounds a number upwards to its nearest integer
# math.floor(value) rounds a number downwards to its nearest integer
# math.pi eturns the value of PI (3.14...)
