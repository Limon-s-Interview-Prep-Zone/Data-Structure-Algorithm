# First import json
# json.loads(value) # convert json to py dictionary
# json.dumps(value) # convert into json
# json.dumps(x,intent=value,separators=(". ", " = "))
# json.dumps(x, indent=4, sort_keys=True)
